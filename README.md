# Python-file-sorter
A Python-based automation script that organizes files in a directory into categorized folders (CSV, images, text files) based on file type. Built using Jupyter Notebook and Python’s OS and Shutil libraries to demonstrate file handling, automation, and basic scripting for workflow optimization.
